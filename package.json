{
  "name": "tesseract-gesture3d",
  "version": "1.0.0",
  "private": true,
  "description": "3D model viewer controlled by webcam hand gestures",
  "license": "MIT",
  "//": "app.js and gestures.js are ES modules loaded directly by the browser. Declaring it here stops Node warning when tests import gestures.js. The server has its own package.json pinning commonjs.",
  "type": "module",
  "engines": {
    "node": ">=20.6"
  },
  "scripts": {
    "setup": "npm install --prefix jnnce-1/server",
    "start": "npm start --prefix jnnce-1/server"
  }
}
